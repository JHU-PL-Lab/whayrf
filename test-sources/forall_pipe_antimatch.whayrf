# EXPECT-TYPECHECK
# EXPECT-EVALUATE

# This example captures the meaning of the pipe operator |>.
# val (|>) : 'a -> ('a -> 'b) -> 'b


pipe = fun parameter -> {
  returnPipe = parameter
};

return = pipe ~ forall beta. forall otherBeta. fun beta ~> { fun fun beta ~> { otherBeta } ~> { otherBeta } } ?
  pipeMatch -> {
    bogusFunctionPipeAntimatch = {};
    parameterForBogusFunctionPipeAntimatch = {};
    returnPipeAntimatch = bogusFunctionPipeAntimatch parameterForBogusFunctionPipeAntimatch
  } : pipeAntimatch -> {
    returnPipeMatch = {}
  }
